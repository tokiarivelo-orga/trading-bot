# Generated by installer/services_linux.py -- do not edit by hand.
# Groups the backend, frontend, and one gateway+terminal pair per enabled
# MT5 account into a single unit `systemctl --user {start,stop,status}
# trading-bot.target` can drive. ${WANTS_LINE} is a space-separated list of
# unit names built in Python (services_linux.py's render_all()) from
# installer/state.json's accounts list -- systemd accepts multiple unit
# names on one Wants=/After= line.
[Unit]
Description=AI Trading Bot -- everything (backend, frontend, per-account MT5 gateway + terminal)
Wants=${WANTS_LINE}
After=${WANTS_LINE}

[Install]
WantedBy=default.target
